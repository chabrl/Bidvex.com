[phases.setup]
nixpkgsArchive = 'latest'
pkgs = ['python311', 'nodejs_20', 'npm']

[phases.install]
cmds = ['pip install -r requirements.txt', 'cd frontend && npm install']

[phases.build]
cmds = ['cd frontend && npm run build']
